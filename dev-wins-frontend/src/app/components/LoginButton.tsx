"use client";
import React from "react";

const LoginButton = () => {
    return (
        <div>
            <button onClick={() => console.log("Login Button Clicked")}>
                Login
            </button>
        </div>
    );
};

export default LoginButton;
